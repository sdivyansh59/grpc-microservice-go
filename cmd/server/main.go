package main

import (
	"fmt"
	"log"
)

func Run() error {
	// responsible for initialization and starting
	// out gRPC server
	return nil
}


func main() {
	if err := Run(); err != nil {
		log.Fatal(err)
	}
	fmt.Println("every thing fine!")
}